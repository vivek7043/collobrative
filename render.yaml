services:
  - type: web
    name: antigravity-server
    env: node
    plan: free
    rootDir: server
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: MONGO_URI
        sync: false
        placeholder: "Paste your MongoDB Atlas connection string here"
      - key: CLIENT_URL
        value: "*"
